<h2><%= I18n.t('page.email.create') %></h2>
<%= form_with model: [ @person, @person.emails.build ] do |form| %>
  <div>
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>
  <div>
    <%= form.submit I18n.t('button.submit'), class: 'button is-info' %>
  </div>
<% end %>